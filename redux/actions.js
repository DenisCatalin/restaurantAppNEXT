import * as types from "./types";

// INCREMENT COUNTER BY 1
export const showNav = () => ({ type: types.SHOW_NAV });

// DECREMENT COUNTER BY 1
export const hideNav = () => ({ type: types.HIDE_NAV });
